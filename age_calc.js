        // References to elements.
        const button
        const input
        const output 
    
        function calculate (string) {
            /* Calcular edad di`a, mes y anno. */
            }


        Element.addEventListener("click", calculate);